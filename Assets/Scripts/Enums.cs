public enum PlayerType {
    Local,
    Remote
}

public enum WordType {
    Noun,
    Pronoun,
    Verb,
    Adjective,
    Adverb,
    Preposition,
    Conjunction,
    Interjection
}

public enum GameState {
    None,
    Menu,
    Connecting,
    Playing,
    PostGame
}

public enum MenuState {
    None,
    Menu,
    Name
}